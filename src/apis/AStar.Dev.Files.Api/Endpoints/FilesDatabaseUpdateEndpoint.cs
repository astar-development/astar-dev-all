namespace AStar.Dev.Files.Api.Endpoints;

public sealed class FilesDatabaseUpdateEndpoint
{
    // the idea here is to add the endpoint to trigger the background dB update but, as it is an endpoint,
    // we can use authorisation to ensure only certain users can trigger
}
